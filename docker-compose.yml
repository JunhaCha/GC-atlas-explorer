services:
  gc-singlecell-explorer:
    build: .
    container_name: gc-singlecell-explorer
    ports:
      - "3838:3838"
    environment:
      GC_APP_DATA_DIR: /data
    volumes:
      - ${GC_APP_HOST_DATA_DIR:-/mnt/ix1/Projects_lite/junhacha/GCshinyapp_data}:/data:ro
    restart: unless-stopped
